# define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'

# Use the working local connection (same DB the fixtures are seeded into),
# matching .env's DATABASE_URL.
DATABASE_URL="mysql://root:root@127.0.0.1:3306/starlinger_reco?serverVersion=8.0&charset=utf8mb4"
